<div class="row justify-content-center">
  <% @body_class = 'layout-login-centered-boxed' %>

  <div class="layout-login-centered-boxed__form card">
    <div class="d-flex flex-column justify-content-center align-items-center mt-2 mb-5 navbar-light">
      <%= link_to root_path, class: "navbar-brand flex-column mb-2 align-items-center mr-0", style: "min-width: 0" do %>
        <span>Mot de passe oublié ?</span>
      <% end %>
    </div>

    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="form-group">
        <label class="text-label" for="email_2">Email :</label>
        <div class="input-group input-group-merge">
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control form-control-prepended' %>
          <div class="input-group-prepend">
            <div class="input-group-text">
              <span class="fa fa-envelope"></span>
            </div>
          </div>
        </div>
      </div>
      <div class="form-group">
        <%= f.submit "Réinitialiser le mot de passe", class:'btn btn-block btn-primary' %>
      </div>
      <div class="form-group text-center">
        <%= render "devise/shared/links" %>
      </div>
    <% end %>
  </div>
</div>
